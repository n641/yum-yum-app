import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

//get all order accepted with 

//accepted function =>{
// edit order with createdAt equal in order with status = delivered
// add prdouct in this oreder in last order of user with edit function if it is not exist already 
// add all order in array of oldorders in user
// }

const getDelivery = () => {
  return (
    <View>
      <Text>getDelivery</Text>
    </View>
  )
}

export default getDelivery

const styles = StyleSheet.create({})